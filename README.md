## Dataset Availability

This repository contains only the benchmark datasets used in the experiments.

The reported results (PTSR values, runtime measurements, and statistical test outputs) are not included as raw files, as they are generated dynamically through the experimental procedure described in the paper.

All results can be reproduced by running the provided experimental setup using the input datasets.
